#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter The Length Of Square :");
    scanf("%d",&a);
    b=a*a;
    printf("the lenght of square is %d and square area is  %d \n",a,b);
    b=4*a;
    printf("the lenght of square is %d and square perimeter is  %d\n ",a,b);
}